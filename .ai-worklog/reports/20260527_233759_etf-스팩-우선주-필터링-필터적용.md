# AI Work Report

## Session ID
20260527_233759_etf-스팩-우선주-필터링-필터적용

## Title
ETF / 스팩 / 우선주 필터링 - 필터적용

## Git User
tjrcksgnl@gmail.com

## Prompt File
.ai-worklog/prompts/20260527_233759_etf-스팩-우선주-필터링-필터적용.md

## 작업 결과
이전 세션(20260527_225511_test, commit bcd6f7e)에서 구현 완료. 이번 세션은 프롬프트·레포트 기록만 적용.

### 구현된 내용 (Collectors.py)

**`_apply_filters(stocks, config)` 함수** — 5개 필터 순차 적용:
1. `config.exclude_keywords` 포함 종목 제외
2. 종목명 `"우"` 결말 → 우선주 제외 (`config.exclude_preferred`)
3. 가격 범위 밖 제외 (`min_price` ~ `max_price`)
4. 등락률 범위 밖 제외 (`min_change_pct` ~ `max_change_pct`)
5. 거래대금 미달 제외 (`amount * 1_000_000 < min_trading_value`)
6. 필터 전/후 출력: `[필터] N개 → M개 (X개 제외)`

**`get_top_gainers(market, config=None)` 함수** — config 전달 시 필터 적용:
```python
if config is None:
    return results, {"before": len(results), "after": len(results)}
return _apply_filters(results, config)
```

### SignalConfig 기본값 (config.py)
| 항목 | 값 |
|---|---|
| min_trading_value | 50억 원 |
| min_change_pct | 5.0% |
| max_change_pct | 30.0% |
| min_price | 1,000원 |
| max_price | 500,000원 |
| exclude_keywords | 스팩, ETF, ETN, 리츠, 인버스, 레버리지 등 |

## Changed Files
- (없음 — 이전 세션에서 기구현)

## Git Diff Stat
```text
(no changes this session)
```

## Suggested Commit Message
[AI] record: ETF/스팩/우선주 필터링 프롬프트 및 레포트 기록

## Review Checklist
- [x] 프롬프트와 실제 변경 내용이 일치하는지 확인
- [x] 불필요한 파일이 변경되지 않았는지 확인
- [ ] 실행 또는 테스트를 완료했는지 확인
- [x] 민감정보가 포함되지 않았는지 확인
